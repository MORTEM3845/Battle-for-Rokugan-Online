# Worker

- Locate exact methods before reading the large `RoomObject.ts` file.
- Preserve `requestQueue`/`patchQueue`, Durable Object names, bindings, migrations, storage, authorization and route contracts.

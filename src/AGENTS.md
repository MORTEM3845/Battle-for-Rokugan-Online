# Client

- Locate exact symbols, selectors or element IDs before reading large `GameBoard.tsx`, `game.css` or map SVG ranges.
- Keep `shared/` contracts and Worker routes authoritative; preserve reconnect, session, localization and map behavior unless in scope.
